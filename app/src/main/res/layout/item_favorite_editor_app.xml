<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/favoriteEditorItemRoot"
    android:layout_width="match_parent"
    android:layout_height="58dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="4dp"
    android:paddingEnd="4dp">

    <ImageView
        android:id="@+id/imgFavoriteEditorIcon"
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:contentDescription="@null" />

    <TextView
        android:id="@+id/txtFavoriteEditorName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="14dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="#F4F7FB"
        android:textSize="15sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/txtFavoriteEditorBadge"
        android:layout_width="wrap_content"
        android:layout_height="34dp"
        android:layout_marginEnd="10dp"
        android:background="@drawable/bg_favorite_badge"
        android:gravity="center"
        android:includeFontPadding="false"
        android:minWidth="92dp"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:text="✓ Favorito"
        android:textColor="#C084FC"
        android:textSize="13sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/btnFavoriteEditorAction"
        android:layout_width="34dp"
        android:layout_height="34dp"
        android:background="@drawable/bg_favorite_action_circle"
        android:gravity="center"
        android:includeFontPadding="false"
        android:text="+"
        android:textColor="#E5E7EB"
        android:textSize="24sp" />

</LinearLayout>